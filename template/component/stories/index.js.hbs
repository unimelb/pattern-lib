import { storiesOf } from '@storybook/vue';
import { createStory } from '.storybook/utils';

import {{properCase name}}Default from './{{properCase name}}Default.vue';

storiesOf('{{titleCase name}}', module)
  .add('Default', createStory({{properCase name}}Default));
