By default all tables are transformed into responsive tables at runtime.  Add the class `table--is-not-responsive` to the `<table>` to define it as a non-responsive table.

### Example:

```
<table class="table--is-not-responsive">
 ...
</table>
```